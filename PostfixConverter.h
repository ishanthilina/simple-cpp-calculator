#include <vector>

#include "IntermediateFormConverter.h"

using namespace std;

class PostfixConverter : public IntermediateFormConverter
{
public:
	vector<string> convert(vector<string>);
protected:
private:
};